# How to use?

1. Clone the repo
```
git clone https://github.com/atrithakar/nginx-starter-pack.git
```

2. Run the command (depending upon your configuration, you may also need to prepend sudo)
```
docker compose up
```